AI Knowledge-Base & Operations Chatbot
======================================

Monorepo containing:
- `apps/web` — Next.js (React, JSX) frontend for chat + admin
- `apps/api` — Node.js backend (chat runtime + ingestion)


